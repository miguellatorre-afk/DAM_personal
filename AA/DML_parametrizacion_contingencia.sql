SET SERVEROUTPUT ON;
BEGIN
    DELETE FROM DETFACTURA WHERE NUMFACTURA IN ('F0001', 'F0002', 'F0003', 'F0004', 'F0005');
    DELETE FROM FACTURA WHERE NUMFACTURA IN ('F0001', 'F0002', 'F0003', 'F0004', 'F0005');
    DELETE FROM CLIENTES WHERE DNI IN ('12345678A', '23456789B', '34567890C', '45678901D', '56789012E');
    DELETE FROM VEHICULOS WHERE MATRICULA IN ('1234ABC', '2345BCD', '3456CDE', '4567DEF', '5678EFG');
    DELETE FROM REPUESTOS WHERE REFERENCIA IN (100001, 100002, 100003, 100004, 100005);
   
    
    

DBMS_OUTPUT.PUT_LINE('Datos de prueba eliminados correctamente.');
    
EXCEPTION
    WHEN OTHERS THEN
        ROLLBACK;
        DBMS_OUTPUT.PUT_LINE('Error eliminando datos: ' || SQLERRM);

END;